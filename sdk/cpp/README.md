# Overlayward C++ SDK

RAII wrapper over the C SDK (`liboverlayward`).

## Generate

```bash
make gen-cpp   # gRPC C++ stubs from proto
make gen-c     # C header from Rust FFI via cbindgen
```
